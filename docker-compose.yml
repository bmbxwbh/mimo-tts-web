version: "3.8"

services:
  mimo-tts-web:
    image: ghcr.io/bmbxwbh/mimo-tts-web:latest
    container_name: mimo-tts-web
    ports:
      - "${PORT:-26645}:26645"
    environment:
      - PORT=26645
      - WORKERS=${WORKERS:-1}
    restart: unless-stopped
